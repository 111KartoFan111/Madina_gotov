import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

// Қолданбаның түбірін құру және оны DOM элементіне рендерлеу
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);